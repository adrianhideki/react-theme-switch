import type { DimensionValues } from "../dimensions/types";
import type { BorderWidthValues } from "./types";

export const borderWidth: Record<BorderWidthValues, DimensionValues> = {
  xs: 25,
  sm: 50,
  md: 100,
};
